# ISTS2026

## Basic git

```
# create branch
git checkout -b "branch_name"

# create commit
git add .
git commit -m "commit message"

# push commit to remote origin
git push

# pull from main into current branch
git pull origin main

# switch to other branch
git checkout other_branch
```
